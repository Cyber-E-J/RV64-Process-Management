useful command in editing .gitignore file

git rm -r --cached . (Clear all the cache)


vim ~/.gitignore_global    
git config --global core.excludesfile ~/.gitignore_global
(add something that is ignored globally )